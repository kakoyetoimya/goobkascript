using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cricket : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Ecco uno script, che verrà ripetuto, se non si vuole scrivere questo script, scriverlo all'inizio di //.
    void Update()
    {
        print("Crickets");
    }
}
